# Paste into ~/.grok/config.toml or project .grok/config.toml
# Requires: pip install "git+https://github.com/mergeos-bounties/mt4-mcp.git"

[mcp_servers.mt4_mcp]
command = "mt4-mcp"
args = ["serve"]
env = { MT4_MCP_MODE = "mock" }
enabled = true
